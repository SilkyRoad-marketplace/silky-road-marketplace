
# SilkyRoad Marketplace (Fixed Next.js Version)

## Deployment Steps

1. Run `npm install`
2. Create `.env.local` file with:
   - RESEND_API_KEY=your_resend_api_key
   - SUPABASE_URL=your_supabase_url
3. Test locally using:
   npm run dev
4. Deploy to Vercel (Framework: Next.js)
